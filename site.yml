- name: Bootstrap fresh Debian server
  hosts: linux
  remote_user: "{{ os_administrator }}"
  gather_facts: true
  roles:
    - bootstrap
    - base
    - firewall

- name: Configure Docker hosts
  hosts: docker_hosts
  become: true
  roles:
    - docker